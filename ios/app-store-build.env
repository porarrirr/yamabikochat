# Release CI toolchain and supported SDK ceiling are independent: local
# Xcode 27 builds may use SDK 27.0 while CI remains on its pinned toolchain.
# This is the project build policy, not a guarantee of App Store acceptance.
app_store_xcode_version=26.5
app_store_max_sdk_version=27.0
