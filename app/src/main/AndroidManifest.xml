<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- SYSTEM_ALERT_WINDOW is a special permission; request via Settings intent at runtime -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:name=".MyApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:networkSecurityConfig="@xml/network_security_config"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.YamabikoChat"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.YamabikoChat"
            android:resizeableActivity="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- FileProvider for sharing code files -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>

        <service
            android:name=".overlay.OverlayService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- "テキストを選択" のコンテキストメニュー（PROCESS_TEXT）に表示するためのエイリアス -->
        <!-- 実体のActivityはUIを持たないプロキシで、選択文字列を受け取りオーバーレイを表示します -->
        <activity
            android:name=".actions.ProcessTextActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:theme="@style/Theme.YamabikoChat.Transparent" />

        <!-- やまびこチャットで質問 -->
        <activity-alias
            android:name=".actions.ProcessTextAsk"
            android:exported="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/process_text_ask_label"
            android:targetActivity=".actions.ProcessTextActivity">
            <intent-filter>
                <action android:name="android.intent.action.PROCESS_TEXT" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
            <meta-data
                android:name="com.porarri.yamabikochat.PROCESS_TEXT_MODE"
                android:value="ask" />
        </activity-alias>

        <!-- やまびこチャットで翻訳 -->
        <activity-alias
            android:name=".actions.ProcessTextTranslate"
            android:exported="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/process_text_translate_label"
            android:targetActivity=".actions.ProcessTextActivity">
            <intent-filter>
                <action android:name="android.intent.action.PROCESS_TEXT" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
            <meta-data
                android:name="com.porarri.yamabikochat.PROCESS_TEXT_MODE"
                android:value="translate" />
        </activity-alias>
    </application>

</manifest>
