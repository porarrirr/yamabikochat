*** Begin Patch
*** Update File: app/src/main/java/com/porarri/yamabikochat/data/local/AppDatabase.kt
@@
         fun getDatabase(context: Context): AppDatabase {
             return INSTANCE ?: synchronized(this) {
                 val instance = Room.databaseBuilder(
                     context.applicationContext,
                     AppDatabase::class.java,
                     "chat_database"
-                ).addMigrations(*AppDatabaseMigrations.ALL_MIGRATIONS).build()  
+                )
+                    .addMigrations(*AppDatabaseMigrations.ALL_MIGRATIONS)
+                    // If the app binary carries an older schema than the on-device DB
+                    // (e.g., 34 -> 32), allow a destructive reset instead of crashing.
+                    .fallbackToDestructiveMigrationOnDowngrade()
+                    .build()
                 INSTANCE = instance
                 instance
             }
         }
     }
 }
*** End Patch